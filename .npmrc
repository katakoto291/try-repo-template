# dependencies' postinstall/prepare scripts are disabled; see README for the
# allowlist mechanism (pnpm-workspace.yaml's allowBuilds) when a script is actually needed
ignore-scripts=true
engine-strict=true

# supply-chain cooldown: refuse to install a package version until it has
# been publicly available for this many minutes (4320 = 3 days)
minimum-release-age=4320
